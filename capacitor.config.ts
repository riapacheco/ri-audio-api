import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.riaudio.app',
  appName: 'ri-audio-api',
  webDir: 'dist/ri-audio-api',
  bundledWebRuntime: false
};

export default config;
